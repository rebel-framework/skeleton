// TODO: Implement routing system
// app.get('/signin', () => {});
import hello from './controllers/hello';

// type Route = {
//   get: (path, controller) => {};
// };

// const route: Route = {};

// route.get('/hello', hello);

hello({});
